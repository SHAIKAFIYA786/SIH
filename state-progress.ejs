<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>State Progress</title>
    <style> 
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #f4f4f4;
    color: #333;
}
 
nav {
    background-color: #ff6600;
    color: white;
    padding: 20px;
    text-align: center;
}

h1 {
    font-size: 24px;
}

 
main {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px;
    padding: 20px;
    margin-top: 20px;
}

.progress-card {
    background: linear-gradient(145deg, #ff9f00, #ff6600);
    color: white;
    padding: 30px;
    border-radius: 10px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    text-align: center;
    font-size: 18px;
    cursor: pointer;
    transform: translateX(-100%); 
    animation: slideIn 0.8s ease forwards; 
}

.progress-card:nth-child(1) {
    animation-delay: 0.1s;
}

.progress-card:nth-child(2) {
    animation-delay: 0.3s;
}

.progress-card:nth-child(3) {
    animation-delay: 0.5s;
}

.progress-card:nth-child(4) {
    animation-delay: 0.7s;
}

 
.progress-card:hover {
    transform: scale(1.05);  
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);  
}

 
@keyframes slideIn {
    100% {
        transform: translateX(0);  
    }
}

 
@media (max-width: 768px) {
    main {
        grid-template-columns: 1fr; 
    }

    .progress-card {
        padding: 20px;
    }
}

    </style>
</head>

<body>
    <nav>
        <h1>State Level Progress</h1>
    </nav>
    <main>
        <div class="progress-card total">
            <h2>Total State Progress</h2>
            <p>Count: <span id="count"><%=10%></span></p>
        </div>
        <div class="progress-card caste">
            <h2>State Progress in Caste Certificates</h2>
            <p>Count: <span id="count"><%=2%></span></p>
        </div>
        <div class="progress-card income">
            <h2>State Progress in Income Certificates</h2>
            <p>Count: <span id="count"><%=5%></span></p>
        </div>
        <div class="progress-card ews">
            <h2>State Progress in EWS Certificates</h2>
            <p>Count: <span id="count"><%=3%></span></p>
        </div>
</body>
</html> -->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>State Progress</title>
    <script>
         
        const state = '<%= state %>';  

        async function fetchCounts() {
            try {
                const response = await fetch(`/user-count/state/${state}`);
                const data = await response.json();
                console.log("Data fetched:", data);
            } catch (error) {
                console.error("Error fetching counts:", error);
            }
        }

        fetchCounts();  


         
        setInterval(fetchCounts, 10000);  

        window.onload = fetchCounts;
    </script>
           /* General styling */
           body {
               font-family: Arial, sans-serif;
               margin: 0;
               padding: 0;
               background-color: #f9f9f9;
               color: #333;
           }
   
           h1 {
               text-align: center;
               margin-top: 20px;
               font-size: 2em;
               color: #4CAF50;
           }
   
           .container {
               display: flex;
               flex-wrap: wrap;
               justify-content: center;
               align-items: center;
               gap: 20px;
               margin: 20px auto;
               max-width: 1200px;
               padding: 10px;
           }
   
           .stat-card {
               background-color: #fff;
               border-radius: 10px;
               padding: 20px;
               width: 250px;
               text-align: center;
               box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
               animation: fadeIn 1.5s ease-in-out;
               transform: scale(0.9);
               transition: transform 0.3s ease;
           }
   
           .stat-card:hover {
               transform: scale(1);
               box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
           }
   
           .stat-card p {
               margin: 10px 0;
               font-size: 1.2em;
               font-weight: bold;
           }
   
           .stat-card:nth-child(1) {
               background: linear-gradient(135deg, #ff9a9e, #fad0c4);
           }
   
           .stat-card:nth-child(2) {
               background: linear-gradient(135deg, #a1c4fd, #c2e9fb);
           }
   
           .stat-card:nth-child(3) {
               background: linear-gradient(135deg, #fbc2eb, #a6c1ee);
           }
   
           .stat-card:nth-child(4) {
               background: linear-gradient(135deg, #d4fc79, #96e6a1);
           }
   
           @keyframes fadeIn {
               from {
                   opacity: 0;
                   transform: translateY(20px);
               }
               to {
                   opacity: 1;
                   transform: translateY(0);
               }
           }
   
           /* Responsive Design */
           @media (max-width: 768px) {
               .stat-card {
                   width: 90%;
               }
           }
       </style>
</head>

<body>
    <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>State Progress</title>
</head>

<body>
    <h1>Progress for State: <%= state %></h1>
    <p>Total Applications: <%= total %></p>
    <p>Caste Applications: <%= caste %></p>
    <p>Income Applications: <%= income %></p>
    <p>EWS Applications: <%= ews %></p>
    <p>compare</p>
</body>

</html>

</body>

</html>